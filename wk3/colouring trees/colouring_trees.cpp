
#include <bits/stdc++.h>
#include <climits>
using namespace std;

#define ll long long 
const int MAXN = 100;
int N, M, K;
ll arr[MAXN];
ll dp[1 << MAXN][MAXN];
ll bonuses[MAXN][MAXN];

int main() {
    cin.tie(nullptr);
    cin.sync_with_stdio(false);
    cin >> N >> M >> K;8
    for (int i = 0; i < N; i++) {
        cin >> arr[i];
    }

}
